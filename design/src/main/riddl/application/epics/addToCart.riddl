epic AddToCartEpic {
  case UserAddsItemToCart is {
    user AuthedUser wants to "Add an item to their cart" so "they can purchase it when ready"
    step take button BuyProductButton from user AuthedUser
    step focus user AuthedUser on page ShippingAddressPage
  }
}